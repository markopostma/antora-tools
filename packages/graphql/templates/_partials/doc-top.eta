= <%= data.title %>
:description: <%= helpers.notBlank(data.description) ? data.description.replace(/[^\w\s]/gi, '').substring(0, 159) : '' %>
:page-pagination:
:caution-caption: Deprecated

<% if (helpers.notBlank(data.description)) { %>
[.lead]
<%= data.description %>
<% } %>

<% if (data.isDeprecated) { %>
CAUTION: <%= data.deprecationReason %>
<% } %>