{
  "name": "antora-tools",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rimraf **/lib",
    "dev": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "rimraf": "^5.0.0"
  }
}
